using System;
/*
using System.Collections.Generic;
using System.Text;
using Db4objects.Db4o;
using SDE.CamadaControle;

namespace SDE.CamadaImportacao
{
    public class SuperImporta //MyDB4O.MySuperNegocioCommit
    {
        private CItem _cItem;
        protected CItem cItem { get { if (_cItem == null)_cItem = new CItem(); return _cItem; } }

        private CEmp _cEmp;
        protected CEmp cEmp { get { if (_cEmp == null)_cEmp = new CEmp(); return _cEmp; } }

        private CCliente _cCliente;
        protected CCliente cCliente { get { if (_cCliente == null)_cCliente = new CCliente(); return _cCliente; } }

        private CPessoa _cPessoa;
        protected CPessoa cPessoa { get { if (_cPessoa == null)_cPessoa = new CPessoa(); return _cPessoa; } }

        private CCorp _cCorp;
        protected CCorp cCorp { get { if (_cCorp == null)_cCorp = new CCorp(); return _cCorp; } }

        private CMov _cMov;
        protected CMov cMov { get { if (_cMov == null)_cMov = new CMov(); return _cMov; } }



        protected IObjectContainer db0
        {
            get
            {
                return banco.get(0);
            }
        }
        protected IObjectContainer db
        {
            get
            {
                if (banco.idBancoEmUso == 0)
                    throw new Exception("Corporação Indeterminada");
                return banco.get(banco.idBancoEmUso);
            }
        }








    }
}
*/