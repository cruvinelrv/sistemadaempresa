using System;
using System.Collections.Generic;
using System.Web;
using System.Web.UI;
using System.Web.UI.WebControls;

using SDE.Entidade;
using Db4objects.Db4o;

namespace SDE.Web
{
    public partial class Corretivos : System.Web.UI.Page
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            Button1.Text = "CListas MS";
        }

        protected void Button1_Click(object sender, EventArgs e)
        {
            /*
            GerenteConectividadeBancoDados bancos = AppFacade.get.conexaoBanco;
            IObjectContainer db = bancos.get(1, GerenteConectividadeBancoDados.TipoBanco.cadastros);

            if (db.Query<CorporacaoListas>().Count > 0)
            {
                Response.Write("<h1>Já foi feito</h1>");
                return;
            }

            CorporacaoListas novos = new CorporacaoListas();
            novos.categorias = new List<Categoria>();
            novos.marcas = new List<Marca>();
            novos.grades = new List<Grade>();

            db.Store( novos );

            db.Commit();
            Response.Write("<h1>concluido</h1>");
             * */
        }
    }
}
