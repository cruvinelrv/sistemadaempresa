package Core.ConexaoExterna
{
	import flash.net.URLRequestMethod;
	
	import mx.rpc.events.FaultEvent;
	import mx.rpc.events.ResultEvent;
	import mx.rpc.http.HTTPService;
	
	public final class BuscaCEP
	{
		/*
		public function Busca(cep:String, fRetorno:Function, fErro:Function):void
		{
			var http:HTTPService = new HTTPService();
			http.method = URLRequestMethod.POST;
			http.addEventListener(ResultEvent.RESULT, function(ev:Object):void{fRetorno(ev.result.webservicecep);});
			http.addEventListener(FaultEvent.FAULT, fErro);
			http.url = "http://www.buscarcep.com.br/?formato=xml&cep=" + cep;
			http.send();
		}
		*/
	}
}